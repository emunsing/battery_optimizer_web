export default {
    testEnvironment: "jsdom",
    // testEnvironment: 'node', // Use Node.js environment for testing
    transform: {}, // Disable transformations (for ESM support)
    // extensionsToTreatAsEsm: [".mjs"], // Treat .js files as ESM
  };